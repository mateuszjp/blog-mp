<!DOCTYPE html>
<html>
<head>
  <title>Blog</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<div>
  <h1><%= link_to 'Blog', posts_path %></h1>
  <div class="content"><%= yield %></div>
  <div class="category">
    <h2><%= link_to 'Categories', cattegories_path %></h2>

    <% @cattegories.each do |cat| %>
        <%= link_to cat.name, cattegory_path(cat) %><br>
    <% end %>
  </div>
<div>

</body>
</html>
